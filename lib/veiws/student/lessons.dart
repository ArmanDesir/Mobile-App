import 'package:flutter/material.dart';

class StudentLessons extends StatelessWidget {
  const StudentLessons({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Lessons Screen', style: TextStyle(fontSize: 24)),
    );
  }
}
