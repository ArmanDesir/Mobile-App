import 'package:flutter/material.dart';

class StudentExercises extends StatelessWidget {
  const StudentExercises({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Exercises Screen', style: TextStyle(fontSize: 24)),
    );
  }
}
