import 'package:flutter/material.dart';

class StudentQuizzes extends StatelessWidget {
  const StudentQuizzes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Quizzes Screen', style: TextStyle(fontSize: 24)),
    );
  }
}
